# Clé admin optionnelle. Vide = auth admin désactivée (CI / démo kind).
# Pour activer : make secret KEY=...  ou éditer puis kubectl apply.
apiVersion: v1
kind: Secret
metadata:
  name: openyard-control
  namespace: openyard
  labels:
    app.kubernetes.io/name: openyard-control
type: Opaque
stringData:
  OPENYARD_API_KEY: ""
